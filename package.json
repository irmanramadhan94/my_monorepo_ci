{
    "name": "my-monorepo",
    "private": true,
    "workspaces": [
      "apps/*",
      "tests/*"
    ],
    "scripts": {
        "web": "npm run start --workspace=apps/web-app",
        "test": "cd tests/cypress && npx cypress run"
}
  }
  